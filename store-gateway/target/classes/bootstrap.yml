server:
  port: 3000 # 前端默认访问端口号为3000
  servlet:
    context-path: / # 前端默认访问的根路径
spring:
  application:
    name: gateway-service  # 服务名
  cloud:
    nacos:
      server-addr: 8.134.149.126:8848 #注册中心